# Not required for the default setup - the extraction pipeline runs against a
# local Ollama server (http://localhost:11434) at zero cost. Only needed if
# you later add a paid-API comparison arm.
ANTHROPIC_API_KEY=
